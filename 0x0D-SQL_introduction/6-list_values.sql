-- list all rows of the table first_table from the db hbtn_0c_0 in MySQL server
-- all fields should be printed
-- the database name will be passed as an arguement of the myql command

SELECT * FROM first_table;
